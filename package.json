{
  "name": "pi-agent-setup",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test --experimental-strip-types tests/auto-continue.test.ts && npm --prefix extensions/background-terminals test && npm --prefix extensions/file-search test"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.82.0",
    "@earendil-works/pi-coding-agent": "^0.82.0",
    "@earendil-works/pi-tui": "^0.82.0",
    "@types/node": "^26.0.0",
    "typebox": "^1.3.6",
    "typescript": "^7.0.2"
  }
}
